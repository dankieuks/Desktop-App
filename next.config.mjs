/** @type {import('next').NextConfig} */
const nextConfig = {
  // Chú thích dòng dưới đây nếu bạn không cần appDir
  // experimental: {
  //   appDir: true,
  // },
  output: "export",
};

export default nextConfig;
